/**
 * declare configurations and export them
 */
module.exports = {

    // database configs
    database : {
        connectionconfig : {
            host : "localhost",
            port : 27017,
            db : "testdb"
        }
    }
}